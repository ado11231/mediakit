{
  "name": "@mediakit-example/source-app",
  "private": true,
  "type": "module",
  "description": "The M1 gate: a project that installs @mediakit/* as a consumer and exercises the extension API with one custom block, one custom layout, and one custom preset.",
  "license": "MIT",
  "scripts": {
    "render": "mediakit render marketing/launch.spec.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@mediakit/blocks": "workspace:*",
    "@mediakit/cli": "workspace:*",
    "@mediakit/core": "workspace:*",
    "@mediakit/render-still": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "vitest": "^4.1.10"
  }
}
