# mediakit

Requires Node.js 22 or newer.

## Install

```bash
npm install -D mediakit
npx mediakit init --quick
```

## Commands

```bash
npx mediakit check
npx mediakit preview
npx mediakit export
```

Use `npx mediakit init` for design discovery or `npx mediakit --help` for options.
